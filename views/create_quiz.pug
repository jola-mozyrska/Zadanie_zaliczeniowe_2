extends layout.pug

block content
    br
    br
    p.msg=message_how_to
    p.msg=example_json
    form(method='POST', action='/create_quiz')
        label(for='quiz') Quiz:
        br
        br
        textarea(cols='40' rows='30' name='quiz')
        br
        br
        input(type='hidden', name='_csrf', value=csrfToken)
        input(type='submit' value='Prześlij')
